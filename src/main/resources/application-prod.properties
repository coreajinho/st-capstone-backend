# ==========================================
# Development Profile Configuration
# ==========================================

# ==========================================
# Server Configuration (Development)
# ==========================================
server.port=8080

# ==========================================
# Logging Configuration (Development)
# ==========================================
logging.level.root=INFO
logging.level.springframework=INFO
logging.level.org.springframework.web=INFO
logging.level.org.example=DEBUG

# ==========================================
# Database Configuration (Development)
# ==========================================
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3308/capstone_main?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=1503
spring.datasource.hikari.maximum-pool-size=10

# ==========================================
# JWT Configuration (Development)
# ==========================================
jwt.secret=yourSecretKeyForJWTTokenGenerationMustBeLongEnoughForHS256Algorithm
jwt.expiration=86400000

# ==========================================
# Riot API Configuration (Development)
# ==========================================
riot.api.key=RGAPI-59d8965c-6035-43d0-a171-1858e25097b8
riot.api.url.kr=https://kr.api.riotgames.com
riot.api.url.asia=https://asia.api.riotgames.com

# ==========================================
# CORS Configuration (Development)
# ==========================================
app.cors.allowed-origins=http://localhost:5173

# ==========================================
# Business Logic Configuration (Development Override)
# ==========================================
# ?? ?? ?? ?? (?????)
debate.expiration=PT24H

# ?? ?? ??: ?? ?? ?? ????? (5??? ??)
debate.expiration-override=PT5M

# ?? ?? ??: ???? ?? ?? (10?)
scheduler.match-check-interval-override=10000

# ???? ?? ?? (?????, 60?)
scheduler.match-check-interval=60000

# ?? ?? ? ?? ?? (?? 1??)
debate.pending-extension-duration=PT1H

# ? ?? ???? (10?)
match.timeout=PT10M
